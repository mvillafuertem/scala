package io.github.mvillafuertem.akka.todo.infrastructure

object ToDoReceptionist {

//  val value: Behavior[Nothing] = Behaviors.setup { context =>
//    val key = ServiceKey[Command]("")
//    val receptionist = context.system.receptionist
//    val actor = context.spawnAnonymous(ToDoBehavior(""))
//    receptionist ! Register(key, actor)
//
//    val value = context.messageAdapter[Command] {
//      Behaviors.receiveMessage[Command] {
//        case key.Listing(a) => GetToDos(a)
//      }
//    }
//    receptionist ! Find(key, adapter)
//    Behaviors.same
//  }

}
